import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 4,
    borderWidth: 1,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 80,
    width: 30,
    height: 30,
    borderColor: "#807a7a",
  },
  text: {
    fontSize: 16,
    color: "#807a7a",
  },
});
